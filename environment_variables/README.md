# Environment Variables

This directory contains example environment variable configuration for the Ubuntu SQL Server project.

Environment variables are used to keep configuration values and sensitive information outside of the source code.

## Example File

The `.env.example` file shows the environment variables used by the project without containing real credentials.

To create a local environment file:

```bash
cp environment_variables/.env.example .env
